#!/usr/bin/env bash

if ! command -v foreman &> /dev/null; then
  echo "Installing foreman..."
  gem install foreman
fi

# Build assets first
yarn build
yarn build:css

# Run with foreman using Procfile.dev
exec foreman start -f Procfile.dev "$@"
